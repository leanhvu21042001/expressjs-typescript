export const sendSingleMail = async (to: string, subject: string, text: string, html: string) => {
  // code to send email here
}

export const sendBulkMail = async (to: string[], subject: string, text: string, html: string) => {
  // code to send email here
}
